---
const timeline = [
  {
    year: "2007",
    title: "Founded",
    content:
      "GopherHost was founded by just two employees designing Real Estate websites for Realtors in the local Phoenix, AZ area.",
  },
  {
    year: "2012",
    title: "Company Growth Phase",
    content:
      "We divided our company into two branches.  Half the company continued website design/development while the other half explored building a website hosting infrastructure.",
  },
  {
    year: "2015",
    title: "Entered Hosting Market",
    content:
      "After several years of continual development and testing, GopherHost released its first official website hosting product.",
  },
  {
    year: "Today",
    title: "Continued Growth",
    content:
      "GopherHost continues its growth and investment into the best hosting product on the market at incredibly affordable  pricing.",
  },
];
---

<div
  class="features-container"
  style="--bg-color: #ebf5ff; --color: var(--clr-blue)"
>
  <section class="features">
    <div class="header-wrapper">
      <header>
        <span class="tagline" style="--bg-color: #cedeef"
          >GOPHERHOST TIMELINE</span
        >
        <h2>Our Short History</h2>
      </header>
    </div>
    <div class="articles">
      {
        timeline.map(({ title, content, year }) => (
          <article>
            <div class="date">{year}</div>
            <div class="figure">
              <div class="circle" />
              <div class="line" />
            </div>
            <h3>{title}</h3>
            <p>{content}</p>
          </article>
        ))
      }
    </div>
  </section>
</div>
<style>
  .features {
    text-align: center;
    background-color: var(--bg-color);
    clip-path: ellipse(93% 100% at 50% 100%);

    header {
      text-align: center;
      display: flex;
      flex-direction: column;
      max-inline-size: var(--container-inline-size);
      margin-inline: auto;
      padding: var(--gap);
      align-items: center;
      gap: var(--gap);
      padding-block: var(--larger-gap);
    }

    article {
      display: grid;
      text-align: left;
      gap: var(--gap);
      .date {
        color: var(--clr-blue);
        font-weight: 700;
        font-size: var(--font-size-fluid-1);
      }

      .figure {
        display: grid;
        align-items: center;
        & > * {
          grid-area: 1/-1;
        }
      }
      .circle {
        background-color: var(--clr-blue);
        width: 1rem;
        height: 1rem;
        border-radius: 9999px;
        z-index: 20;
      }

      .line {
        border-bottom: 1px solid var(--gray-4);
      }

      h3 {
        margin-block-start: var(--gap);
        font-size: var(--font-size-2);
        font-weight: 500;
      }

      p {
        padding-right: var(--large-gap);
        font-size: var(--font-size-0);
        color: var(--gray-6);
        line-height: var(--font-lineheight-4);
      }
    }

    .articles {
      max-inline-size: var(--container-inline-size);
      margin-inline: auto;
      display: grid;
      /* gap: var(--larger-gap); */
      align-items: start;
      padding: var(--large-gap);
      grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
    }

    header p {
      font-size: var(--font-size-fluid-0);
      color: var(--gray-6);
      line-height: var(--font-lineheight-4);
    }

    h2 {
      font-size: var(--font-size-fluid-1);
      font-weight: 600;
    }
  }
</style>
